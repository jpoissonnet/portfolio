import type { NextPage } from "next";
import styled from "styled-components";

const Container = styled.div`
  margin: 0 2rem;
`;

const Home: NextPage = () => {
  return (
    <Container>
    </Container>
  );
};

export default Home;
